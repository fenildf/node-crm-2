extends ../layout

block body
	.page-header 
		h1 Auth&nbsp;
			small Login
	form(method="post")
		fieldset
			legend Login details
			.col
				input(type="text", name="name", placeholder="Login name", required)
				input(type="password", name="password", placeholder="Password", required)
				input.btn.btn-primary.pull-right(type="submit", value="Login")